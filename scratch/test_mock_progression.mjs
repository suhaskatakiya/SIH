// Author: suhas katakiya | Enrollment: 24BIT214D
// scratch/test_mock_progression.mjs

import { createMockClient } from '../apps/web/src/lib/services/adapters/mock.ts';

// We can test the mock adapter in node by running tsx or node with loader if needed,
// but let's check if tsx is available or run via vitest
